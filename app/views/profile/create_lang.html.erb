<%= form_for(@languages_user, :as => 'create_lang') do |f| %>
  <% if @languages_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@languages_user.errors.count, "error") %> prohibited this language from being saved:</h2>

      <ul>
      <% @languages_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :language_id %><br>
    <%= f.text_area :language_id %>
  </div>
  <div class="field">
    <%= f.collection_select(:language_id, Language.all, :id, :language) %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

